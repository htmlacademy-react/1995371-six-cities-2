enum ClassnameActionMode {
  Add = 'Add',
  Remove = 'Remove'
}

enum SortActionMode {
  Open = 'Open',
  Close = 'Close'
}

enum PlaceCardMode {
  Default = 'default',
  Favorite = 'favorite'
}

enum HeaderMode {
  LoginScreen = 'loginScreen',
  Default = 'default'
}

enum RatingViewMode {
  Card = 'place-card',
  Offer = 'offer',
  Reviews = 'reviews'
}

enum PremiumMarkMode {
  Card = 'place-card',
  Offer = 'offer',
}

enum BookmarkButtonMode {
  Card = 'place-card',
  Offer = 'offer',
}

enum PriceViewMode {
  Card = 'place-card',
  Offer = 'offer',
}

export {
  ClassnameActionMode,
  SortActionMode,
  PlaceCardMode,
  HeaderMode,
  RatingViewMode,
  PremiumMarkMode,
  BookmarkButtonMode,
  PriceViewMode,
};
